{
  "name": "@musestruct/backend",
  "private": true,
  "scripts": {
    "dev": "cargo-watch -x run",
    "build": "cargo build",
    "lint": "cargo check",
    "test": "cargo test",
    "check": "cargo check"
  }
}