{
  "name": "musestruct",
  "private": true,
  "packageManager": "npm@10.9.8",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "backend": "turbo run dev --filter=@musestruct/backend",
    "flutter": "turbo run dev --filter=@musestruct/flutter",
    "react-native": "turbo run dev --filter=@musestruct/react-native",
    "docs": "turbo run dev --filter=@musestruct/docs",
    "android": "turbo run android --filter=@musestruct/react-native",
    "android:install": "turbo run android:install --filter=@musestruct/react-native",
    "ios": "turbo run ios --filter=@musestruct/react-native",
    "build": "turbo run build",
    "docs:build": "turbo run build --filter=@musestruct/docs",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "check": "turbo run check"
  },
  "devDependencies": {
    "@react-native-community/cli": "20.1.0",
    "@react-native-community/cli-server-api": "20.1.0",
    "turbo": "^2.5.4"
  }
}